---
description: Write this session's lessons into the project's lessons/ directory using the memory-discipline format.
---

Run a short retrospective of this session and persist what is worth remembering.

1. Load the `memory-discipline` skill and follow its write rules.
2. Identify candidate lessons: user corrections, approaches that worked after failed
   attempts, non-obvious project facts discovered the hard way. Exclude anything the
   repo already records.
3. For each lesson: create/update a file in `lessons/` (one lesson per file, one-line
   summary first), then refresh `MEMORY.md` index lines.
4. Show the user a one-line-per-lesson summary of what was saved.
