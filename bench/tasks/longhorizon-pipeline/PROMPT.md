Build the three-stage log analysis pipeline described in SPEC.md as a single module
`pipeline.py`. The stages depend on each other — build and verify them in order.
`sample.log` is a sample input. Acceptance is judged against the spec, all stages.

Work fully autonomously: do not ask questions; make reasonable assumptions and note
them. You are done only when the stated success criteria hold. Do not modify or
delete existing tests unless the task says so.
